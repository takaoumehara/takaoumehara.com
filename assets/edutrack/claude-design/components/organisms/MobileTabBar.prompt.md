Bottom tab bar replacing the desktop masthead on phones; 44px+ targets, safe-area aware.

```jsx
<MobileTabBar active={view} onNavigate={setView} style={{ position: 'fixed', bottom: 0, left: 0, right: 0 }} />
```
