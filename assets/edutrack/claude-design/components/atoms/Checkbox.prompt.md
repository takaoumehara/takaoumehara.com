Square green-fill checkbox whose checkmark pops in with the reward easing.

```jsx
<Checkbox checked={done} onChange={setDone} strike label="POV anchor chart" />
```

Use `strike` for checklists where checking = consumed (materials to prep).
