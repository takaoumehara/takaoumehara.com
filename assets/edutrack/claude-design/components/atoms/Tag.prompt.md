Small uppercase label for standards codes and selectable options.

```jsx
<Tag>5R1 — Inference & Evidence</Tag>
<Tag variant="chip" selected={grade === '5'} onClick={() => setGrade('5')}>5</Tag>
```

Variants: outline (metadata), solid (inline on colored rows), chip (selectable; selected = accent fill).
