# Content audit — 2026-09-26

## Clients A

Agent E1, 2026-09-26. Ten client pages. All ten already used the shared detail template (`"format": "detail"`) and a real still for the teaser. The thin part was the body: 1–3 beats of bare images, with no beat title and no text (Fire TV had only a disclaimer caption). Each page now has 2–3 full beats (`h2.beat-title`, `p.beat-text` EN+JP, `figure.beat-media` with real images and a bilingual caption). Every claim in them comes from one of three places: the deck, the preserved legacy pages, or text that is literally visible in the work images.

Nothing was lost. The richest hand-built version of each page is restored from git to `src/case-studies/_legacy/<slug>.{html,css,json}`. `[slug].astro` does not glob that folder. The six pages compressed in 1725f53 were restored from `1725f53^`; tmobile, carnegie and odell-education came from `7047172^`. The pre-Astro originals are still in `git show 838d498^:projects/<slug>.html`.

| Page (/projects/…) | Before → after (beats · words in main) | Sources used | Remaining gaps |
|---|---|---|---|
| verizon-ai-agents | 3 beats, 1 diagram + 7 portraits, deck-derived text → 3 beats, 7 real deck slides (heat map, Master Brain inputs, Justin 4-layer scan, Sally's knowledge, WDS team, workflow before/after) · 1294 → 1468 words. Solution now says "16 designers" instead of "16+", following slide 4. Teaser unchanged: `hero-agents-network.jpg` | Takao's "AI Powered Workflow Training Workshop" deck. The published Google Slides link is unreachable from the sandbox, but its 57-page PDF export was deleted in e1c14c9 and is restored from `e1c14c9^:assets/verizon-ai-agents/Verizon_Work_Transformation.pdf`. Also `docs/verizon-work-transformation-slides.md` and `_legacy/verizon-ai-agents.html` | The deck has no measured KPI, so none is claimed. Slides naming client projects, brands or dollar figures (15, 27–29, 39, 53–54: Flex Pay, SUPER Sku, Zelda, StraightTalk, a $113M figure, the 41/100 audit) were left out on purpose. Clara and Alex have no slide imagery |
| verizon-totalwireless | 1 beat (2 images, caption only) → 3 beats: flow map + prototype link, cart/checkout, coded design system + styleguide link · 846 → 1264 | `_legacy/verizon-totalwireless.html`, the Figma flow map (web JPG made from the 8405px PNG), and the orphaned live-coded prototypes and styleguide already in `public/assets/verizon-totalwireless/` (Prototype/*.html, storybook/). Stack now includes React / Next.js / shadcn/ui | The prototypes load Tailwind and React from CDNs, so they could not be screenshotted here (egress is blocked). They are linked instead. `final-web-html/02.html` and `03.html` are scraped copies of the live totalwireless.com page and were deliberately not used |
| amazon-firetv | 1 beat (disclaimer caption only) → 2 beats: dual-device simulator, snap-pair and state mirroring, Buy Again screen · 852 → 1178 | `_legacy/amazon-firetv.html`, a capture of the orphaned simulator `public/projects/amazon-firetv/demo.html`, and `orders.jpg` restored from `e1c14c9^` | The walkthrough video lives on Cloudinary (external), so it is not embedded. The voice overlay could not be captured without a mic |
| tmobile | 2 beats, caption only → 3 beats: three walkthrough videos, journey maps, sketch → flow → 3D kiosk · 840 → 1227 | `_legacy/tmobile.html`; `public/assets/tmobile/*`. The ten kiosk steps are read off `Kiosk-mini-wire.png` | No year beyond 2024. Team contribution is by title only |
| coca-cola | 2 beats, caption only → 2 beats with text: Animal World (4 images), SketchVibe World (6 images) · 859 → 1120 | `_legacy/coca-cola.html`; `public/assets/coca-cola/*` | Record has no year (the filenames suggest 2019, which is not recorded). `Card_5115_…jpeg` in that folder is Kitadoko stationery, so it is not used |
| credit-card-portal | 2 beats, caption only → 2 beats: four priority flows, the three-layer system and its adoption · 855 → 1146 | `_legacy/credit-card-portal.html`; `nobrand_Bankcard.*`, `shared/Designsystems.00*` | "Service calls decreased" is qualitative on the legacy page, so it is not claimed |
| xq | 2 beats, caption only → 3 beats: Canvas, Directory, hub · 845 → 1209 | `_legacy/xq.html`; `xq-canvas/*`, `xq-directory/*`. The Canvas stages and application steps are read off the images | No year and no participation numbers (none are stated). The YouTube films are external, so they are not linked |
| carnegie | 2 beats, caption only → 3 beats: concept test, guided conversation, full interaction flow · 857 → 1205 | `_legacy/carnegie.html`; `public/assets/carnegie/*` | The 40% / 25% figures are "stated" but it is unclear whether they are measured or projected (existing `_notes`). Only 40% is repeated in the body |
| extraordinary | 2 beats, caption only → 3 beats: origin story, photography and layout, LA launch + DESIGN QUARTERLY · 858 → 1189 | `_legacy/extraordinary.html`; `public/assets/extraordinary/*`. The Solution was corrected: the brush spine belongs to the handmade prototype, not the published edition | Amazon purchase links are external, so they are not on the page |
| odell-education | 3 beats, caption only → 3 beats: grade system + Montserrat, unit covers, IA → website · 849 → 1126 | `_legacy/odell-education.html`; `public/assets/odell-education/*` | The stylescape and website-capture videos exist but are unused (the 3-beat cap). No metrics exist |

Verification (dev server, 2026-09-26):
- All ten pages return 200 and show one h1 carrying the name only.
- Beat counts are 2–3, and every `src`, `poster` and `href` in the beats returns 200.
- There are no external links in the article except Fire TV's Play link.
- Screenshots `E1-*.png` are in the scratchpad.
- `node --test tests/lens-system.test.mjs` passes 29/29.
- The beats' JP spans were checked by hand-script for banned katakana and Latin/JP spacing.
