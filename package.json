{
  "name": "takaoumehara.com",
  "private": true,
  "type": "module",
  "description": "Adaptive career portfolio — one evidence base, rendered through lenses. No dependencies.",
  "scripts": {
    "generate": "node src/build.mjs",
    "check": "node src/build.mjs --check",
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 4173"
  },
  "engines": { "node": ">=20" }
}
