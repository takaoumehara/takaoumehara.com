# ムードボード/ポートフォリオギャラリー仕様書

**プロジェクト**: Awesome Animation - Mood Board Feature  
**日付**: 2026-04-23  
**ステータス**: 仕様策定

---

## 📋 概要

ポートフォリオサイトのプロジェクト一覧から画像を抽出し、Moodボードビューで表示するページを実装。devouringdetails.com/mood を参考にした、インタラクティブなポートフォリオギャラリー。

---

## 🎯 機能要件

### 1. 画像収集機能
- ポートフォリオ内のプロジェクトから画像を自動抽出
- サポート形式: JPG, PNG, WebP
- メタデータ: プロジェクトタイトル、プロジェクトID、サムネイル画像URL

### 2. ギャラリー表示
- **グリッドレイアウト**: 複数列のレスポンシブグリッド
  - デスクトップ: 3-4列
  - タブレット: 2列
  - モバイル: 1列
- **画像最適化**: Next.js Image コンポーネントを使用
- **遅延読み込み**: スクロールに合わせて画像を段階的に読み込む

### 3. インタラクティブ機能
- **ホバー効果**: 画像にズーム/フェードイン
- **カード展開**: クリックしてプロジェクト詳細を表示
  - プロジェクトタイトル
  - 簡単な説明
  - 「詳細を見る」リンク
- **フィルター機能** (オプション):
  - プロジェクトカテゴリ別フィルター
  - タグベースのフィルター

### 4. 詳細ページへのリンク
- 各画像カードから対応するポートフォリオページへのリンク
- リンク: `/portfolio/[projectId]` または外部URL
- 新規タブで開く、または同ページ遷移

### 5. キャンバスビュー/フルスクリーン表示
- 画像をクリックしてフルスクリーン/モーダル表示
- 前後の画像に移動できるナビゲーション
- 詳細情報の表示（プロジェクト名、説明）
- 「ポートフォリオページを開く」ボタン

---

## 🎨 ビジュアル/UI仕様

### レイアウト
```
┌─────────────────────────────────────────┐
│        MOOD BOARD / PORTFOLIO            │
│        (ページタイトル & 説明)           │
├─────────────────────────────────────────┤
│  [Filter] [Category] [Sort]  [View]     │  ← コントロール
├─────────────────────────────────────────┤
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐       │
│  │ IMG │ │ IMG │ │ IMG │ │ IMG │       │
│  │[1]  │ │[2]  │ │[3]  │ │[4]  │       │
│  └─────┘ └─────┘ └─────┘ └─────┘       │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐       │
│  │ IMG │ │ IMG │ │ IMG │ │ IMG │       │
│  └─────┘ └─────┘ └─────┘ └─────┘       │
└─────────────────────────────────────────┘
```

### カードスタイル
- 背景: 白/濃いグレー（ダークモード対応）
- ボーダー: 微妙なシャドウ
- ホバー: 画像ズーム（105%）+ シャドウ増加
- 遷移時間: 300-400ms

### フルスクリーン/モーダル
- 背景: 黒（透明度: 80-90%）
- 画像: 中央表示、最大幅/高さ制限
- ナビゲーション: 前・次ボタン（両側）
- クローズボタン: 右上
- キーボード対応: ← → で画像移動, Esc で閉じる

---

## 🛠️ 技術仕様

### スタック
- **フレームワーク**: Next.js 15+ (App Router)
- **UI フレームワーク**: React 19+
- **画像最適化**: Next.js Image
- **アニメーション**: 
  - CSS Transitions (基本アニメーション)
  - Framer Motion (複雑なアニメーション) または CSS Animations
- **状態管理**: React Hooks (useState, useReducer)
- **スタイリング**: Tailwind CSS または CSS Modules

### ファイル構成
```
src/
├── app/
│   ├── mood/
│   │   ├── page.tsx          # メインムードボードページ
│   │   ├── layout.tsx        # ムードボード専用レイアウト
│   │   └── styles.css        # ムードボード用スタイル
│
├── components/
│   ├── MoodBoard/
│   │   ├── MoodGallery.tsx      # ギャラリーメインコンポーネント
│   │   ├── ImageCard.tsx        # 個別画像カード
│   │   ├── FullScreenModal.tsx  # フルスクリーン表示モーダル
│   │   ├── FilterBar.tsx        # フィルターコントロール (オプション)
│   │   └── moodboard.css        # スタイル
│
├── lib/
│   ├── portfolio.ts          # ポートフォリオデータ取得
│   ├── imageExtractor.ts     # 画像抽出ロジック
│   └── types.ts              # 型定義
│
└── data/
    └── projects.json         # プロジェクトメタデータ (仮)
```

### データ構造

#### Project メタデータ
```typescript
interface ProjectImage {
  id: string;                          // ユニークID
  projectId: string;                   // プロジェクトID
  projectTitle: string;                // プロジェクト名
  projectDescription?: string;         // プロジェクト説明
  imageUrl: string;                    // 画像URL
  category?: string;                   // カテゴリ (e.g., "UI/UX", "Web Design")
  tags?: string[];                     // タグ配列
  portfolioPageUrl: string;           // ポートフォリオページURL
  width: number;                       // 画像の幅
  height: number;                      // 画像の高さ
  order?: number;                      // 表示順序
}

interface MoodBoardData {
  projects: ProjectImage[];
  totalCount: number;
  categories: string[];
}
```

### API エンドポイント (内部)
- `GET /api/portfolio/images` - ポートフォリオ画像一覧取得
- `GET /api/portfolio/images/[id]` - 特定画像の詳細取得

### アニメーション仕様
1. **ページ読み込み**: フェードイン (300ms)
2. **画像カード**: 
   - ホバー: スケール 1 → 1.05 (250ms ease-out)
   - シャドウ増加 (250ms)
3. **モーダル開閉**: 
   - オープン: フェードイン + スケール 0.8 → 1.0 (300ms)
   - クローズ: フェードアウト + スケール 1.0 → 0.8 (250ms)
4. **画像移動**: フェードイン/アウト (200ms)

---

## 📱 レスポンシブ仕様

| デバイス | グリッド列数 | 画像サイズ | コントロール配置 |
|---------|-----------|---------|---------------|
| モバイル (< 640px) | 1列 | 100%幅 | スタック |
| タブレット (640px - 1024px) | 2列 | 50%幅 | 水平 |
| デスクトップ (> 1024px) | 3-4列 | 25-33%幅 | 水平 |

---

## 🔄 ユーザーフロー

### メインフロー
1. ユーザーが `/mood` ページにアクセス
2. グリッドレイアウトに画像が読み込まれる
3. ユーザーが画像にホバー → 効果が表示
4. ユーザーが画像をクリック:
   - **オプション A**: フルスクリーン/モーダル表示
   - **オプション B**: 直接ポートフォリオページへ遷移
5. フルスクリーンの場合:
   - ← → ボタンで次の画像に移動
   - 「ポートフォリオページを開く」ボタンで詳細ページへ移動
   - Esc キーでモーダルを閉じる

### フィルターフロー (オプション)
1. フィルターバーでカテゴリ/タグを選択
2. グリッドが自動更新
3. マッチする画像のみ表示

---

## ✅ 実装チェックリスト

### フェーズ 1: 基本構成
- [ ] データ構造・型定義を確定
- [ ] ポートフォリオプロジェクトデータを集約
- [ ] 画像抽出ロジック実装
- [ ] API エンドポイント作成

### フェーズ 2: UI/コンポーネント
- [ ] MoodGallery コンポーネント実装
- [ ] ImageCard コンポーネント実装
- [ ] グリッドレイアウト + レスポンシブ対応
- [ ] Tailwind/CSS でスタイリング

### フェーズ 3: インタラクション
- [ ] ホバー/クリック効果実装
- [ ] モーダル/フルスクリーン実装
- [ ] キーボード操作対応
- [ ] アニメーション調整

### フェーズ 4: 最適化 & 仕上げ
- [ ] 画像最適化 (Next.js Image)
- [ ] パフォーマンス測定
- [ ] アクセシビリティ対応 (WCAG)
- [ ] ダークモード対応

---

## 🎬 アニメーション例

### ホバーアニメーション (CSS)
```css
.image-card {
  transition: transform 250ms ease-out, box-shadow 250ms ease-out;
}

.image-card:hover {
  transform: scale(1.05);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
}
```

### モーダル開閉 (Framer Motion 例)
```tsx
<motion.div
  initial={{ opacity: 0, scale: 0.8 }}
  animate={{ opacity: 1, scale: 1 }}
  exit={{ opacity: 0, scale: 0.8 }}
  transition={{ duration: 0.3 }}
>
  {/* モーダルコンテンツ */}
</motion.div>
```

---

## 📝 今後の拡張案

- [ ] 検索機能 (プロジェクト名で検索)
- [ ] ソート機能 (日付, 人気度)
- [ ] シェア機能 (SNS共有)
- [ ] いいね機能 / ブックマーク
- [ ] コメント機能
- [ ] AI生成タグ自動付与
- [ ] 画像キャプション自動生成

---

## 📞 参考リンク

- **参考サイト**: https://devouringdetails.com/mood
- **技術スタック**:
  - Next.js: https://nextjs.org
  - Tailwind CSS: https://tailwindcss.com
  - Framer Motion: https://www.framer.com/motion/
