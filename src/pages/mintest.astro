---
interface Props { proofRef: any; item: any; index: number }
const { proofRef, item, index } = Astro.props;
function metricRow(metricIds) {
  return "no nested backticks here";
}
---
<div>{metricRow([])}</div>
